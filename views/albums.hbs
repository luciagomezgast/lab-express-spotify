{{!-- {{#each items}}
<div>
<img src="{{images.0.url}}">
<a href="/tracks/{{id}} ">View Tracks</a>
</div>
{{/each}}
 --}}

 <h1> Research results</h1>
<div class="row">
    {{#each items}}
  <div class="col-md-4 ">
    <div class="thumbnail bg-dark p-2 g-col-6 mt-2">
<p class=" fs-4 text-white">{{name}}</p>
        <img src="{{images.0.url}}" alt="Lights" style="width:100%" />
        <div class="caption"> 
          <a href="/tracks/{{id}}" class="btn btn-danger">Listen some music!</a>
        </div>
      </a>
    </div>
  </div>
   {{/each}}
</div>